import styled from 'styled-components';

export const HomeWrapper = styled.div`
  width: 100%;
  max-width: 100%;
`;

export const ProductWrapper = styled.div`
  padding: 0 16px;
`;

export const HeaderWrapper = styled.div`
  position: sticky;
  top: 0;
  z-index: 9;
`;

export const FabButtonWrapper = styled.div`
  position: fixed;
  right: 16px;
  bottom: 16px;
`;
